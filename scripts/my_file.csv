import requests
import json
from collections import defaultdict

# Fetch WFS features (GeoJSON)
url = "https://geoserver-2-27-vector-dev.apps.j1lk3njp.eastus.aroapp.io/geoserver/core/wfs?service=WFS&version=2.0.0&request=GetFeature&typeNames=buildings&outputFormat=application/json&count=100"
response = requests.get(url)
data = response.json()

# Build mapping from property name to list of distinct values
property_values = defaultdict(set)

for feature in data.get("features", []):
    for prop, value in feature.get("properties", {}).items():
        property_values[prop].add(value)

# Convert sets to sorted lists
result = {k: sorted(list(v)) for k, v in property_values.items()}

# Print or save
print(json.dumps(result, indent=2))
